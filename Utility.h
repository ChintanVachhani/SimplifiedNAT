#ifndef SIMPLIFIEDNAT_UTILITY_H
#define SIMPLIFIEDNAT_UTILITY_H

#include <vector>
#include <string>
#include <sstream>

using namespace std;

class Utility {
public:
    static vector<string> splitString(string, char);
    static string trimString(string);
};

#endif //SIMPLIFIEDNAT_UTILITY_H